# Python 3.12 runtime dependencies for the ROS 2 Jazzy nodes.
# ROS 2 packages (rclpy, cv_bridge, tf2_ros, launch, launch_ros and message
# packages) are installed by the Ubuntu/ROS 2 apt packages, not by pip.

numpy>=1.24,<2
opencv-python>=4.8,<5
Pillow>=10,<12
PyYAML>=6,<7
pytest>=7,<9
